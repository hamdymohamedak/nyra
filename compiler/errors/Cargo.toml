[package]
name = "errors"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Nyra compiler diagnostics and source spans"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
