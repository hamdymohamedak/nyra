fn main() -> void {
    let nums: [i32; 3] = [10, 20, 30]
    for n in nums {
        print(n)
    }

    let label: string = "Nyra"
    print(label.length())
    for ch in label {
        print(ch)
    }
}
