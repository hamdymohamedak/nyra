name = "openssl"
description = "TLS / cryptography toolkit"
headers = ["openssl/ssl.h"]
libs = ["ssl", "crypto"]
pkg_config = "openssl"
brew = "openssl@3"
apt = "libssl-dev"
pacman = "openssl"
dnf = "openssl-devel"
aliases = ["ssl"]
